name: styled_widget
description: Simplifying your widget tree structure by defining widget using methods. Taking ispiration from CSS and SwiftUI
version: 0.4.8
homepage: https://github.com/f69/styled_widget
repository: https://github.com/f69/styled_widget

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^2.0.0
